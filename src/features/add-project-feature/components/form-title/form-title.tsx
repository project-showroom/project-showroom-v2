import { FormTitleElement } from '../../../../components/form-elements';

const COMPONENT_NAME = 'FormTitle';
const FormTitle = () => {
  return <FormTitleElement>Add Project</FormTitleElement>;
};

FormTitle.displayName = COMPONENT_NAME;

export { FormTitle };
