import EditIcon from '@mui/icons-material/Edit';

const COMPONENT_NAME = 'EditIconElement';
const EditIconElement = () => {
  return <EditIcon fontSize="small" />;
};

EditIconElement.displayName = COMPONENT_NAME;

export { EditIconElement };
